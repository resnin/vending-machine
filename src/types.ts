export interface IMachineItems {
    title: string
    img: string
    limit: number
    cost: number
}

export interface INominal {
    nominal: number
    limit: number
}

export interface IPurchasedItems {
    title: string
    img: string
    count: number
}